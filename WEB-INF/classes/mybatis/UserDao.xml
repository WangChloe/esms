<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.plan.a.es.dao.UserDao"> 
	
	
	<select id="findById" resultType="com.plan.a.es.entity.User">
	<![CDATA[
		select id, user_no as userNo, name, dorm, mobile, pwd, crt, upt
		from user
		where id=#{id}
	]]> 
	</select>
	
	<select id="findByUserNo" resultType="com.plan.a.es.entity.User">
	<![CDATA[
		select id, user_no as userNo, name, dorm, mobile, pwd, crt, upt
		from user
		where user_no=#{userNo}
		limit 1
	]]> 
	</select>
	
	<select id="findByMobile" resultType="com.plan.a.es.entity.User">
	<![CDATA[
		select id, user_no as userNo, name, dorm, mobile, pwd, crt, upt
		from user
		where mobile=#{mobile}
		limit 1
	]]> 
	</select>
	
	<insert id="save" parameterType="com.plan.a.es.entity.User" >
	<![CDATA[
		insert into user (user_no, name, dorm, mobile, pwd, crt, upt) 
		values (#{user.userNo}, #{user.name}, #{user.dorm}, #{user.mobile}, #{user.pwd}, now(), now())
	]]> 
	</insert>
	
	<update id="updatePwd">
	<![CDATA[
		update user 
		set pwd=#{pwd}
		where id=#{id}
	]]> 
	</update>
	
</mapper>